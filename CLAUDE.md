@AGENTS.md

# กฎเฉพาะโปรเจกต์ bigcat (เว็บแก๊ง BIGCAT · bigcathouse.com)

## 🖼️ ภาพทุกไฟล์ที่เอาเข้าโปรเจกต์ต้องผ่านการย่อ/บีบก่อนเสมอ (แด๊ดสั่ง 14 ก.ย. 2026)

**ทุกครั้ง**ที่เพิ่มหรือเปลี่ยนไฟล์ภาพใน `public/` (โปสเตอร์ · รูปสินค้า · ภาพประกอบ · OG image) ต้องรัน

```
/usr/bin/python3 tools/optimize-images.py <ไฟล์หรือโฟลเดอร์>          # ทำจริง
/usr/bin/python3 tools/optimize-images.py <ไฟล์หรือโฟลเดอร์> --dry    # ดูก่อนว่าจะทำอะไร
```

ก่อน commit — ห้ามเอาไฟล์ดิบจาก codex/กล้อง/Canva (มัก 1.5–2 MB, มี metadata + C2PA) เข้ามาตรง ๆ

**หลักคิด = คำนึงถึงการใช้งานของภาพ ไม่ใช่บีบทุกใบให้เล็กสุด** (ค่าที่ใช้อยู่ใน `PROFILES` ของสคริปต์):

| หน้าที่ของภาพ | โปรไฟล์ (เลือกจากพาธอัตโนมัติ) | ทำอะไร |
|---|---|---|
| ภาพใหญ่ไว้ดูเต็มจอ — hero / สไลด์ / โปสเตอร์ที่คนกดขยาย | `hero` (`*hero*`, `cozy/*desktop*`, `*-mobile*`) | คุณภาพสูง ย่อเฉพาะที่เกินเพดาน **ห้ามบีบจนเห็น artifact** |
| รูปสินค้าที่ต้องซูมดูรายละเอียด | `product` (`public/images/products/`) | 1200px · q82 |
| โลโก้ · ไอคอน · favicon · QR | `logo` (`*logo*` `*icon*` `*favicon*` `*qr*`) | **ไม่แตะเลย** (ลายเส้นคม/โปร่งใส/ต้องสแกนได้) |
| ภาพประกอบทั่วไป / OG image | `ui` (ที่เหลือ) | 1600px · q80 |

ถ้าพาธเดาโปรไฟล์ผิด ใส่ `--profile=hero|product|logo|ui` เอง

**สิ่งที่สคริปต์รับประกัน**: ต้นฉบับถูกสำรองที่ `tools/_originals/` (git-ignored) ครั้งแรกเสมอ · ลบ metadata ทั้งหมด ·
ไม่ทับถ้าผลลัพธ์ใหญ่กว่าเดิม · PNG ที่ไม่มี alpha จะ**ไม่**ถูกแปลงเป็น JPG เว้นแต่ใส่ `--rename`
(แปลงแล้วสคริปต์แก้พาธที่อ้างใน `src/` `server/` `public/` ให้ แต่ถ้าพาธถูกเก็บใน DB — เช่น `products.image` — ต้องแก้เองและเช็คก่อน)

**เช็คหลังทำ**: `git status` ดูว่าไม่มีไฟล์ถูกเปลี่ยนชื่อโดยไม่ตั้งใจ · เปิดหน้าที่ใช้ภาพนั้นดูจริงหนึ่งครั้ง

## deploy / เซิร์ฟเวอร์ — อ่าน `README.md` หัวข้อ deploy
* push `main` = deploy ขึ้น EC2 อัตโนมัติ (GitHub Actions) · `.env` บน EC2 rsync ไม่แตะ
* แก้ `.env` บน EC2 แล้วต้อง `pm2 delete` + `pm2 start ecosystem.config.cjs --only <app>` + `pm2 save`
  (`pm2 restart --update-env` **ไม่**เห็นค่าใหม่ เพราะ pm2 จำ env ตอน start ครั้งแรก และ dotenv ไม่ทับ)
* ตรวจสลิป: `SLIP_PROVIDER=slipok` + `SLIP_AUTO_APPROVE=false` = ตรวจแล้วยังรอแอดมินกดยืนยัน (ตาข่ายชั้นสอง) — เปิด auto เมื่อแด๊ดสั่งเท่านั้น
